apiVersion: v1
kind: Service
metadata:
  name: __APP_NAME__
  namespace: __NAMESPACE__
  labels:
    app: __APP_NAME__
    version: "__VERSION__"
spec:
  selector:
    app: __APP_NAME__
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
